namespace CoreEscuela.Entidades
{
    public enum TiposEscuela
    {
        // ? Si no le especificamos el orden con un número (Primaria = 1)
        // ? El asume que es en orden númerico (0,1,2,3,4,5)
        Primaria,
        Secundaria,
        PreEscolar
    }
}